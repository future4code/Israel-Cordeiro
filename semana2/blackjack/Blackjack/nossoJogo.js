/**
 * EXEMPLO DE UTILIZAÇÃO DA 'comprarCarta'
 * 
 * 
    const carta = comprarCarta(); // Sorteia uma carta. Por exemplo, o rei de ouros
    
    console.log(carta.texto) // imprime o texto da carta. Exemplo: "K♦️" (indica "K" de ouros)
    console.log(carta.valor) // imprime o valor da carta (um número). Exemplo: 10 (dado que "K" vale 10)
 * 
 * 
 * 
 */
/*const iniciandoJogo = () => {
   const confirmacaoUsuario= confirm(' Bem vindo(a) ao jogo de Blackjack, para continuar clique em: ok, se não: cancel.') 
   
   if (confirmacaoUsuario) {
      console.log ('Iniciar uma rodada')

      const carta= comprarCarta(); 

      const usuarioCarta1 = comprarCarta()
      const usuarioCarta2 = comprarCarta()
      const computadorCarta1 = comprarCarta()
      const computadorCarta2 = comprarCarta()

      const resultadoUsuario = usuarioCarta1.valor + usuarioCarta2.valor
      const resultadoComputador = computadorCarta1.valor + computadorCarta2.valor

      console.log(`Usuário - cartas: ${usuarioCarta1.texto} ${usuarioCarta2.texto} -pontuação ${resultadoUsuario}`) 
      console.log (`Computador - cartas: ${computadorCarta1.texto} ${computadorCarta2.texto} -pontuação ${resultadoComputador}`)

      if (resultadoUsuario > resultadoComputador) {
         console.log ('O usuário Ganhou!!')
      }else if (resultadoUsuario < resultadoComputador){
         console.log ('O Computador Ganhou!!')
      }else if (resultadoUsuario === resultadoComputador) {
         console.log ('Empate!')
      }        
   }else{
      console.log ('O Jogo Acabou')
   }
}

iniciandoJogo()*/
     

      

      